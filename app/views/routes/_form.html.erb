<%= form_for(@route) do |f| %>
  <% if @route.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@route.errors.count, "error") %> prohibited this route from being saved:</h2>

      <ul>
      <% @route.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :color_one %><br />
    <%= f.text_field :color_one %>
  </div>
  <div class="field">
    <%= f.label :color_two %><br />
    <%= f.text_field :color_two %>
  </div>
  <div class="field">
    <%= f.label :color_three %><br />
    <%= f.text_field :color_three %>
  </div>
  <div class="field">
    <%= f.label :difficulty %><br />
    <%= f.text_field :difficulty %>
  </div>
  <div class="field">
    <%= f.label :is_live %><br />
    <%= f.check_box :is_live %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
