<%= form_for(@completed_route) do |f| %>
  <% if @completed_route.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@completed_route.errors.count, "error") %> prohibited this completed_route from being saved:</h2>

      <ul>
      <% @completed_route.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
